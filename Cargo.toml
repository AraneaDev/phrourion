[package]
name = "phrourion"
version = "0.6.0"
edition = "2024"
rust-version = "1.85"
description = "A terminal watch post for local repositories, remote branches, checks, and releases"
license = "MIT"
repository = "https://github.com/AraneaDev/phrourion"
keywords = ["git", "tui", "github", "repositories", "monitor"]
categories = ["command-line-utilities", "development-tools"]

[[bin]]
name = "phrourion"
path = "src/bin/phrourion.rs"

[[bin]]
name = "phro"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
crossterm = "0.28"
dirs = "6"
fs2 = "0.4"
keyring = "3"
libc = "0.2"
ratatui = { version = "0.29", features = ["unstable-rendered-line-info"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tempfile = "3"
tokio = { version = "1", features = ["full"] }
toml = "0.8"
url = "2"

[profile.release]
lto = "thin"
strip = true
